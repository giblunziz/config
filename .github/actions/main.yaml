name: "Display info"
on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
jobs:
  print-info-version:
    runs-on: linux
    steps:
      - run: echo "Deploying new version ${{ github.event.inputs.version }}"
